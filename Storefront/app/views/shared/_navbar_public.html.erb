<nav class="navbar">
  <div class="navbar-container">
    <div class="navbar-brand">
      <%= link_to root_path, class: "logo" do %>
        Avivas
      <% end %>
    </div>

    <div class="navbar-menu">
    </div>

    <% if user_signed_in? %>
      <%= link_to "Admin", admin_path, class: "nav-link admin-link" %>
    <% end %>
    
    <div class="navbar-user-menu">
    <% if user_signed_in? %>

        <div class="menu-icon">
            <!-- Ícono SVG -->
            <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                <path d="M20.8 21a8.38 8.38 0 0 0-7.8-4.5h-2c-3.6 0-6.7 2-7.8 4.5"></path>
                <circle cx="12" cy="7" r="4"></circle>
            </svg>
        </div>

    
        <!-- Menú desplegable -->
        <div class="dropdown-menu">
        <%= link_to edit_user_registration_path, class: "dropdown-item" do %>
            Editar Perfil
        <% end %>
        <%= form_with url: destroy_user_session_path, method: :delete, class: "dropdown-item", local: true do |form| %>
            <%= form.submit "Cerrar Sesión", class: "nav-link" %>
        <% end %>
        </div>
    <% else %>
            <%= link_to "Iniciar Sesión", new_user_session_path, class: "nav-link" %>
    <% end %>
    </div>
  </div>
</nav>